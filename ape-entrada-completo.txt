   program:
	
declare int fatorialrecursivo(int n) 

  int resposta;

  if (n <= 1) 
    resposta = 1;
  else
    resposta = n * fatorialrecursivo(n-1);
  endif

  return resposta;

enddeclare


declare int fatorialiterativo(int n)  
  int fatorial;

  fatorial = 1;

  return fatorial;
enddeclare

declare void teste(int a)

  while (n > 0) 
    fatorial = fatorial * n;
    n = n - 1;
  endwhile

  if((a > 0)&&(!(b!=7)))
    a = 2;
  endif

enddeclare

   execute:

int fatorial10recursivo;
int fatorial10iterativo;
fatorial10recursivo = fatorialrecursivo(10);
fatorial10iterativo = fatorialiterativo(10);

   endprogram
